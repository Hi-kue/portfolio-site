---
interface Props {
	children?: React.ReactNode;
	className?: string;
	id?: string;
	isTranslucent: boolean;
}

const { children, className, id, isTranslucent } = Astro.props;
---

<!-- TODO: HTML For :: Nav.astro  w/TailwindCSS -->