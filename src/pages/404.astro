---
interface Props {
    error?: string;
    errorDescription?: string;
    default404: boolean;
}

const { error, errorDescription, default404 } = Astro.props;
---

<!-- TODO: Complete 404.astro --> 