---
interface Props {
    title?: string;
    description?: string;
    sections?: Array<{
        title: string;
        description: string;
        items: Array<{
            title: string;
            description: string;
            image: string;
        }>;
    }>;
}

const {
    title,
    description,
    sections
}: Props = Astro.props;
---

<!-- TODO: project.astro w/TailwindCSS -->